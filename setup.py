# pip freeze > requirements.txt
#* -- Imports
import os

os.system("pip install -r requirements.txt") #? Install requirements
os.system("pause") #? Send "pause" to system (Press any key to continue . . .)
